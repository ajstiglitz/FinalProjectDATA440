from src.interface import main
from pathlib import Path

if __name__ == "__main__":
    app = main()
    # probably not right, need to add buttons or smth to check
    #app.setStyleSheet(Path('style.qss').read_text())